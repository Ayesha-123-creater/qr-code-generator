# qr-code-generator
This project is a simple QR Code Generator built using Python. It takes a user-input link or text and converts it into a QR code image. The program uses the qrcode library to generate and save the QR code as qr.png, which can be scanned to access the embedded data easily.
